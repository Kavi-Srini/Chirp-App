<%= form_with(model: chirp) do |form| %>
  <% if chirp.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(chirp.errors.count, "error") %> prohibited this chirp from being saved:</h2>

        <ul>
          <% chirp.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
  <% end %>



<div class="row">
      <div class="col-3">
          
          <div class="form-group">
          <%= form.text_area :message, placeholder: "What's on your mind?", class:"form-control"%>
          </div>
          
          <div class="col">
            <div class="field">
              <%= form.hidden_field :user_id, value: current_user.id %>
            </div>
          </div>
          
      
      </div>
</div>

<div class="row">
  <div class="col auto">
    <div class="actions">
         <%= form.submit "Chirp it!", class: "btn btn-success"%>
     </div>
  </div>
     
</div>
      

      
  
<% end %>
